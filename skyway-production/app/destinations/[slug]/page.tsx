import {notFound} from 'next/navigation';
const destinations:any={paris:{name:'Paris',country:'France',description:'Flights, hotels, attractions, local transport and trip planning for Paris.'}};
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const d=destinations[slug];return d?{title:`${d.name} Travel Guide — Skyway`,description:d.description}:{} }
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const d=destinations[slug];if(!d)return notFound();return <main className="min-h-screen bg-[#F4F8FA] p-8"><div className="max-w-5xl mx-auto bg-white rounded-3xl p-8 shadow"><p className="text-sm text-[#16697A]">SKYWAY DESTINATION</p><h1 className="text-5xl font-black text-[#0A192F] mt-2">{d.name}</h1><p className="text-slate-600 mt-3">{d.country} · {d.description}</p><div className="grid md:grid-cols-3 gap-4 mt-8">{['Flights','Hotels','7-day itinerary'].map(x=><div className="p-5 rounded-2xl bg-[#F4F8FA]" key={x}><b>{x}</b><p className="text-sm text-slate-500 mt-2">Skyway travel intelligence and booking options.</p></div>)}</div></div></main>}
